<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".feature.dictionary.DictionaryFragment">

    <TextView
        android:id="@+id/tv_dic_title"
        style="@style/main_title"
        android:text="용어 사전"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <Spinner
        android:id="@+id/spinner_dic"
        android:layout_width="90dp"
        android:layout_height="wrap_content"
        android:padding="0dp"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:background="@drawable/spinner_background"
        app:layout_constraintStart_toStartOf="@id/tv_dic_title"
        app:layout_constraintTop_toBottomOf="@id/tv_dic_title"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_dic"
        style="@style/recycler"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/spinner_dic"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>